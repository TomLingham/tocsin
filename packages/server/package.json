{
  "name": "@tocsin/server",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "types": "dist/types",
  "scripts": {
    "build": "babel -d ./lib -x .ts --delete-dir-on-start ./src",
    "watch": "nodemon -w ./src -e .ts ./lib/index.js",
    "dev": "npm run -s build && concurrently 'npm:build -- -w --skip-initial-build' 'npm:watch'"
  },
  "devDependencies": {
    "@koa/router": "^10.0.0",
    "@types/cron": "^1.7.2",
    "@types/koa": "^2.11.6",
    "@types/koa__router": "^8.0.3",
    "concurrently": "^5.3.0",
    "cron": "^1.8.2",
    "koa": "^2.13.0",
    "nodemon": "^2.0.6"
  }
}
